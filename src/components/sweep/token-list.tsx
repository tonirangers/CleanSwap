// This component is kept for backwards compatibility but the main UI
// now uses sweep-widget.tsx which has the token list built in.
// This can be removed once sweep-widget is confirmed working.

export {}
